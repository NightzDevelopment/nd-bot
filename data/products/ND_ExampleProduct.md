# ND_ExampleProduct

Replace this file with real product names, e.g. `ND_DiscordUnified.md`, `ND_Scenes.md`.

## Summary

One or two sentences: what the script does and who it is for.

## Requirements

- Framework: ESX / QBCore / standalone (edit)
- Dependencies: ox_lib, oxmysql, etc.

## Install

1. Copy the resource into your `resources` folder.
2. Add `ensure ND_ExampleProduct` after dependencies in `server.cfg`.
3. Import SQL if the package includes `*.sql`.
4. Configure `config.lua` / `config.json` before starting.

## Configuration highlights

| Option | Purpose |
|--------|---------|
| `Example.Setting` | Describe what it does |

## Common issues

- **Issue:** ... **Fix:** ...

## Links

- Tebex: `https://...`
- Changelog: ...
